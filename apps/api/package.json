{
  "name": "@verivote/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn verivote_api.main:app --app-dir src --reload --host 0.0.0.0 --port 3001",
    "build": "python -m compileall src/verivote_api",
    "start": "python -m uvicorn verivote_api.main:app --app-dir src --host 0.0.0.0 --port 3001",
    "typecheck": "python -m compileall src/verivote_api"
  }
}
